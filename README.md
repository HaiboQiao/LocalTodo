# LocalTodo

LocalTodo 是一款面向 Windows 的本地离线任务管理应用。它不需要账号或云服务，待办数据、每周计划、成长记录、备份和日志都保存在应用所在文件夹中，便于携带、备份和迁移。

## 当前版本

- 版本：2.1.0
- 发布日期：2026-09-03
- 数据库结构：v12
- 支持平台：Windows 10/11 x64
- 运行方式：.NET 10 自包含发布，无需另行安装 .NET
- 发布结构：压缩单文件主体 + 6 个必要原生运行库

## 下载与运行

1. 下载或克隆本仓库。
2. 打开 `LocalTodo-2.1.0-win-x64-portable` 文件夹。
3. 保留文件夹中的全部 7 个程序文件。
4. 双击 `LocalTodo.exe` 启动。

程序目前没有数字签名。Windows 第一次运行时可能显示安全提示，请确认文件来自本仓库，并可使用本文后面的 SHA-256 校验值验证文件完整性。

## 主要功能

### 任务管理

- 新建、查看、编辑、完成、恢复和删除任务
- 任务标题、说明、截止日期、截止时间和重点标记
- 四象限分类以及独立四象限视图
- 所有任务、日历、已完成和垃圾箱视图
- 到期提醒以及多种提前提醒时间
- 每天、每周、每月、每年和工作日循环任务
- 阳历、农历日期显示和指定日期快速新增
- 主窗口、桌面任务列表和桌面四象限之间的数据同步
- 自动保存、多窗口编辑保护和字段级安全合并
- 系统托盘、关闭主窗口后驻留和开机自启动

### 每周计划

- 以七列时间轴安排周一至周日的不同时段
- 拖动计划主体调整日期和时间，拖动上下边缘调整起止时间
- 按住 `Ctrl` 拖动计划可实时预览并复制到其他可放置时段
- 选中计划后按 `Delete` 删除，点击其他位置取消选中
- 自定义计划颜色，自动阻止同一天中的时间冲突

### 成长记录

- 记录已经获得的成果、成果描述以及开始和完成日期
- 在跨年度时间轴上查看成长历程
- 拖动记录移动时间段，拖动左右边缘调整起止日期
- 跨越当前窗口边界时自动滚动时间轴
- 自定义成果分类、名称和配色
- 打开成果详情后可直接编辑，点击应用外部自动关闭

## 2.1.0 更新说明

- 新增完整的每周计划页面及拖动、缩放、复制、键盘删除交互
- 新增成长记录时间轴、跨年度浏览、拖动调整和自定义分类
- 更新主界面视觉样式、任务编辑窗口、按钮和重点任务选择框
- 优化今日任务、逾期任务和日期状态的辨识度
- 修复新增任务窗口失去激活后无法继续输入的问题
- 删除左侧不再需要的数据维护区域
- 数据库由 v8 升级到 v12，并保留旧任务数据的自动迁移和升级前备份

## 为什么发布目录仍有 6 个 DLL

LocalTodo 的托管程序集、第三方托管依赖和应用资源已经合并并压缩到 `LocalTodo.exe` 中。剩余 6 个 DLL 是 WPF 图形、输入和 SQLite 数据库必须直接加载的原生库。

如果继续把这些原生库强行放进 EXE，程序运行时仍需要把它们解压到 Windows 临时目录，这与“程序文件都留在应用文件夹中”的便携要求冲突。当前的 1 个 EXE + 6 个 DLL 是兼顾独立运行、目录整洁和稳定性的最精简方案。

## 数据位置

首次运行后，LocalTodo 会在程序目录中按需创建运行数据文件夹：

```text
LocalTodo-2.1.0-win-x64-portable/
├─ LocalTodo.exe
├─ D3DCompiler_47_cor3.dll
├─ e_sqlite3.dll
├─ PenImc_cor3.dll
├─ PresentationNative_cor3.dll
├─ vcruntime140_cor3.dll
├─ wpfgfx_cor3.dll
├─ Data/
│  └─ localtodo.db
├─ Backups/
└─ Logs/
```

`Data`、`Backups` 和 `Logs` 不会上传到 GitHub。迁移程序时，请复制整个应用文件夹，确保任务数据与程序一起迁移。

## 旧数据库兼容

- 当前数据库结构版本为 v12。
- 已验证加入“每周计划”和“成长记录”之前的 v8 数据库可以自动升级到 v12。
- 升级前会先检查数据库完整性，并在 `Backups` 中创建一致性备份。
- v8 升级只新增新功能所需的数据表，不会重建或删除原有任务表。
- 数据库升级是单向升级；升级后的 v12 数据库不应再交给只支持旧结构的程序使用。

## 更新建议

更新前请：

1. 从系统托盘完全退出 LocalTodo。
2. 备份原应用目录中的 `Data` 和 `Backups`。
3. 替换 7 个程序文件，保留原有运行数据目录。
4. 启动新版本并确认任务、每周计划和成长记录正常。

请勿在程序运行期间替换 `localtodo.db`，也不要只复制数据库文件而遗漏关联的备份目录。

## 文件校验

发布目录共 7 个程序文件，合计 82,688,612 字节。

```text
A05F99734F7C4822FEFC12B367AF21FD0976ED6608752FB1E1E80B6ECE7ECBBB  D3DCompiler_47_cor3.dll
B7385D722C83FB52142A00477A726723745916D22A555711EE89834C1111FB2E  e_sqlite3.dll
533AD5ABFD996C19E9E4CB75E80FDABDFF4F36EE086F279EB1946020570A61D0  LocalTodo.exe
8052E85CD5A3AE9A939872B32DE5B90A827456A21D8CCFC4D0C76599F1FCC217  PenImc_cor3.dll
42E2E543C17E1B37499BAF00D956433B8AFB655348EC0FAE89C926FB7D149B6E  PresentationNative_cor3.dll
D5E4D9A3E835FA679450145D6A7D94E36573A509317111904D9B3712C30D9066  vcruntime140_cor3.dll
0BA67759D290F5D75FF92979394077DCD533FE484511DB3EA161D7E102246A73  wpfgfx_cor3.dll
```

PowerShell 校验示例：

```powershell
Get-FileHash .\LocalTodo-2.1.0-win-x64-portable\LocalTodo.exe -Algorithm SHA256
```

## 隐私

LocalTodo 的任务管理、提醒和数据库操作均在本机完成，不包含账号系统、云同步、遥测或远程数据上传功能。

## 许可证

本项目使用 Apache License 2.0，完整条款和版权声明分别见发布仓库中的 `LICENSE` 与 `NOTICE`。
